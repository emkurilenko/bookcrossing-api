server:
  port: 9191

spring:
  jpa:
    properties:
      hibernate:
        jdbc.time_zone: UTC

spring.datasource.url: jdbc:postgresql://${POSTGRES_DB_HOST}
spring.datasource.username: ${POSTGRES_USER}
spring.datasource.password: ${POSTGRES_PASSWORD}